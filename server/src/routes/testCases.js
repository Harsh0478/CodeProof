import { Router } from 'express'; import { requireAuth } from '../middleware/auth.js'; import { asyncHandler } from '../utils/asyncHandler.js'; import { list,create,update,remove,runOne,runAll } from '../controllers/testCaseController.js'; const r=Router(); r.use(requireAuth); r.post('/',asyncHandler(create)); r.get('/:translationId',asyncHandler(list)); r.put('/:id',asyncHandler(update)); r.delete('/:id',asyncHandler(remove)); r.post('/:id/run',asyncHandler(runOne)); r.post('/translation/:translationId/run-all',asyncHandler(runAll)); export default r;
