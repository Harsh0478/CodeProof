import { Router } from 'express'; import { run,save,list,getOne,remove } from '../controllers/translationController.js'; import { requireAuth } from '../middleware/auth.js'; import { asyncHandler } from '../utils/asyncHandler.js'; const r=Router(); r.use(requireAuth); r.post('/run',asyncHandler(run)); r.post('/',asyncHandler(save)); r.get('/',asyncHandler(list)); r.get('/:id',asyncHandler(getOne)); r.delete('/:id',asyncHandler(remove)); export default r;
