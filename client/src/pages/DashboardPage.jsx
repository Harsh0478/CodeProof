import {Link} from 'react-router-dom'; import {ArrowUpRight,BookOpenCheck,CheckCircle2,Clock3,Languages,Plus,ShieldCheck,XCircle} from 'lucide-react'; import PageHeader from '../components/PageHeader'; import StatCard from '../components/StatCard'; import StatusBadge from '../components/StatusBadge'; import {useAuth} from '../context/AuthContext'; import {api} from '../services/api'; import {useEffect,useState} from 'react';
export default function DashboardPage(){const{user}=useAuth();const[data,setData]=useState({total:0,verified:0,failed:0,averageScore:0,recent:[]});useEffect(()=>{api.get('/dashboard/stats').then(r=>setData(r.data.stats)).catch(()=>{})},[]);return <div className="mx-auto max-w-7xl"><PageHeader eyebrow="Workspace" title={`Good to see you, ${user?.name?.split(' ')[0]||'there'}.`} description="Turn legacy code into a migration that has executable evidence behind it." action={<Link to="/translate" className="inline-flex items-center gap-2 rounded-xl bg-violet-600 px-4 py-2.5 text-sm font-bold text-white shadow-lg shadow-violet-200"><Plus size={17}/> New Translation</Link>}/><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><StatCard label="Total translations" value={data.total} sub="Saved in your workspace" icon={Languages}/><StatCard label="Verified" value={data.verified} sub="90%+ behavioral score" icon={ShieldCheck} tone="emerald"/><StatCard label="Needs review" value={data.failed} sub="Failed or blocked runs" icon={XCircle} tone="rose"/><StatCard label="Average score" value={`${data.averageScore}%`} sub="Across executable runs" icon={BookOpenCheck} tone="amber"/></div><div className="mt-6 grid gap-6 xl:grid-cols-[1.5fr_.7fr]"><section className="rounded-2xl border border-slate-200 bg-white shadow-sm dark:border-slate-800 dark:bg-[#111a2c]"><div className="flex items-center justify-between border-b border-slate-100 px-5 py-4 dark:border-slate-800"><div><h2 className="font-extrabold">Recent translations</h2><p className="text-xs text-slate-500">Your latest saved migration evidence.</p></div><Link to="/history" className="text-xs font-bold text-violet-600">View all</Link></div><div className="divide-y divide-slate-100 dark:divide-slate-800">{data.recent.length?data.recent.map(t=><div key={t._id} className="flex flex-wrap items-center gap-4 px-5 py-4"><div className="flex-1"><div className="font-bold">{t.sourceLanguage} <span className="px-1 text-slate-300">→</span> {t.targetLanguage}</div><div className="mt-1 text-xs text-slate-500">{new Date(t.createdAt).toLocaleString()}</div></div><div className="text-sm font-extrabold">{t.verificationScore??0}%</div><StatusBadge status={t.status}/><Link to={`/results?id=${t._id}`} className="rounded-lg p-2 text-slate-400 hover:bg-slate-100 hover:text-violet-600 dark:hover:bg-slate-800"><ArrowUpRight size={16}/></Link></div>):<div className="p-10 text-center text-sm text-slate-500">No saved translations yet. Start with a sample migration.</div>}</div></section><section className="rounded-2xl border border-violet-100 bg-gradient-to-br from-violet-600 to-indigo-600 p-6 text-white shadow-xl shadow-violet-100 dark:border-violet-400/10 dark:shadow-none"><div className="grid h-10 w-10 place-items-center rounded-xl bg-white/10"><Clock3 size={18}/></div><h3 className="mt-5 text-xl font-extrabold">What makes CodeProof different?</h3><p className="mt-2 text-sm leading-6 text-violet-100">AI review is only one signal. Your final score is grounded in actual compilation, execution and output comparison.</p><div className="mt-6 space-y-3 text-sm"><div className="flex gap-2"><span>✓</span> Primary + backup AI providers</div><div className="flex gap-2"><span>✓</span> Behavioral regression tests</div><div className="flex gap-2"><span>✓</span> Isolated code execution</div></div></section></div></div>}
