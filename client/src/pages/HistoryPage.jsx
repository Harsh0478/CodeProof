import {useEffect,useState} from 'react'; import {Eye,Search,Trash2} from 'lucide-react'; import {Link} from 'react-router-dom'; import PageHeader from '../components/PageHeader'; import StatusBadge from '../components/StatusBadge'; import EmptyState from '../components/EmptyState'; import {api,apiMessage} from '../services/api';
export default function HistoryPage(){const[data,setData]=useState({items:[],pagination:{}}),[q,setQ]=useState(''),[page,setPage]=useState(1),[error,setError]=useState('');const load=()=>api.get('/translations',{params:{q,page,limit:10}}).then(r=>setData(r.data)).catch(e=>setError(apiMessage(e)));useEffect(()=>{load()},[page,q]);const del=async id=>{if(!confirm('Delete this saved translation?'))return;await api.delete(`/translations/${id}`);load()};return <div className="mx-auto max-w-7xl"><PageHeader eyebrow="Workspace" title="Translation history" description="Search, inspect and remove saved migration runs."/><div className="mb-4 flex items-center gap-3"><div className="relative max-w-md flex-1"><Search size={16} className="absolute left-3 top-3.5 text-slate-400"/><input value={q} onChange={e=>{setPage(1);setQ(e.target.value)}} className="w-full rounded-xl border border-slate-200 bg-white py-3 pl-10 pr-4 text-sm dark:border-slate-800 dark:bg-[#111a2c]" placeholder="Search language pair or status"/></div></div>{error&&<div className="mb-4 rounded-xl bg-rose-50 p-3 text-sm text-rose-700">{error}</div>}<div className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm dark:border-slate-800 dark:bg-[#111a2c]"><div className="overflow-auto"><table className="w-full min-w-[780px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wider text-slate-500 dark:bg-[#0c1526]"><tr><th className="px-5 py-3">Source</th><th className="px-5 py-3">Target</th><th className="px-5 py-3">Date</th><th className="px-5 py-3">Score</th><th className="px-5 py-3">Status</th><th className="px-5 py-3">Actions</th></tr></thead><tbody className="divide-y divide-slate-100 dark:divide-slate-800">{data.items.map(t=><tr key={t._id}><td className="px-5 py-4 font-bold">{t.sourceLanguage}</td><td className="px-5 py-4 font-bold">{t.targetLanguage}</td><td className="px-5 py-4 text-xs text-slate-500">{new Date(t.createdAt).toLocaleString()}</td><td className="px-5 py-4 font-extrabold">{t.verificationScore??0}%</td><td className="px-5 py-4"><StatusBadge status={t.status}/></td><td className="px-5 py-4"><div className="flex gap-1"><Link to={`/results?id=${t._id}`} className="rounded-lg p-2 text-slate-500 hover:bg-slate-100 hover:text-violet-600 dark:hover:bg-slate-800"><Eye size={16}/></Link><button onClick={()=>del(t._id)} className="rounded-lg p-2 text-slate-500 hover:bg-rose-50 hover:text-rose-600 dark:hover:bg-rose-400/10"><Trash2 size={16}/></button></div></td></tr>)}</tbody></table>{!data.items.length&&<EmptyState title="No saved translations" text="Run a migration and choose Save Result to populate your history."/>}</div><div className="flex items-center justify-between border-t border-slate-100 px-5 py-3 text-xs text-slate-500 dark:border-slate-800"><span>Page {data.pagination.page||1} of {data.pagination.pages||1}</span><div className="flex gap-2"><button disabled={page<=1} onClick={()=>setPage(p=>p-1)} className="rounded-lg border px-3 py-1.5 disabled:opacity-40">Previous</button><button disabled={page>=(data.pagination.pages||1)} onClick={()=>setPage(p=>p+1)} className="rounded-lg border px-3 py-1.5 disabled:opacity-40">Next</button></div></div></div></div>}
