import {useState} from 'react'; import Sidebar from '../components/Sidebar'; import Topbar from '../components/Topbar';
export default function AppLayout({children}){const[menu,setMenu]=useState(false);return <div className="min-h-screen bg-[#f7f9fc] dark:bg-[#09111f]"><div className="flex min-h-screen"><Sidebar open={menu} onClose={()=>setMenu(false)}/><div className="flex min-w-0 flex-1 flex-col"><Topbar onMenu={()=>setMenu(true)}/><main className="min-w-0 flex-1 p-4 md:p-6">{children}</main></div>{menu&&<button aria-label="Close navigation" onClick={()=>setMenu(false)} className="fixed inset-0 z-40 bg-slate-950/50 lg:hidden"/>}</div></div>}
