export default function ScoreRing({score=0,size=150,label}){const pct=Math.max(0,Math.min(100,score||0));const r=52,c=2*Math.PI*r,d=(pct/100)*c;return <div className="relative" style={{width:size,height:size}}><svg width={size} height={size} viewBox="0 0 120 120" className="-rotate-90"><circle cx="60" cy="60" r={r} fill="none" stroke="currentColor" strokeWidth="10" className="text-slate-200 dark:text-slate-700"/><circle cx="60" cy="60" r={r} fill="none" stroke="currentColor" strokeWidth="10" strokeLinecap="round" strokeDasharray={`${d} ${c-d}`} className={pct>=90?'text-emerald-400':pct>=50?'text-amber-400':'text-rose-400'}/></svg><div className="absolute inset-0 grid place-items-center text-center"><div><div className="text-3xl font-extrabold tracking-tight text-slate-900 dark:text-white">{Math.round(pct)}%</div>{label&&<div className="mt-0.5 text-[10px] font-bold uppercase tracking-widest text-slate-500">{label}</div>}</div></div></div>}
